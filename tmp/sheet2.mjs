import sharp from "sharp"; import { readdirSync } from "node:fs";
const files = readdirSync("tmp/sheet2").filter(f => /\.(webp|png)$/.test(f)); const W = 720, H = 640, COLS = 2; const tiles = [];
for (let i = 0; i < files.length; i++) { const img = await sharp("tmp/sheet2/" + files[i]).resize(W, H - 30, { fit: "contain", background: "#0b0d16" }).png().toBuffer(); const label = Buffer.from(`<svg width="${W}" height="30"><rect width="100%" height="100%" fill="#151a2b"/><text x="8" y="21" font-size="15" fill="#e8ecf8" font-family="sans-serif">${i + 1}. ${files[i]}</text></svg>`); tiles.push({ input: await sharp({ create: { width: W, height: H, channels: 4, background: "#0b0d16" } }).composite([{ input: img, top: 0, left: 0 }, { input: label, top: H - 30, left: 0 }]).png().toBuffer(), left: (i % COLS) * W, top: Math.floor(i / COLS) * H }); }
await sharp({ create: { width: W * COLS, height: H * Math.ceil(files.length / COLS), channels: 4, background: "#000" } }).composite(tiles).png().toFile(process.argv[2]); console.log(files.join(" "));
